name: flutter_job_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.2 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  pinput:
  flutter_launcher_icons:
  get:
  intl:
  shimmer:
  get_storage:
  iconsax:
  firebase_app_check:
  flutter_plugin_android_lifecycle: ^2.0.20
  lottie:
  flutter_native_splash:
  cached_network_image:
  smooth_page_indicator:
  carousel_slider:
  image_picker:
  connectivity_plus:
  image_cropper:
  url_launcher:
  flutter_svg:
  permission_handler:
  uuid:
  rename_app:
  fluttertoast:
  flutter_screenutil: ^5.9.3
  flutter_windowmanager:
  data_table_2: ^2.5.15
  badges: ^3.1.2
 

#  FIREBASE DATABASE PLUGIN
  firebase_auth:
  firebase_core:
  cloud_firestore:
  firebase_storage:




dev_dependencies:
  flutter_test:
    sdk: flutter


flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/ChemisphereLogo.png"



flutter_native_splash:
  color: "#42a5f5"
  branding_mode: bottom
  background_image: "assets/images/ChemisphereFlash.png"

#  flutter_lints: ^4.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/onboarding/
    - assets/animation/


  fonts:
    - family: CircularStd
      fonts:
        - asset: fonts/CircularStd-Black.ttf
        - asset: fonts/CircularStd-BlackItalic.ttf
        - asset: fonts/CircularStd-Bold.ttf
        - asset: fonts/CircularStd-BoldItalic.ttf
        - asset: fonts/CircularStd-Book.ttf
        - asset: fonts/CircularStd-BookItalic.ttf
        - asset: fonts/CircularStd-Medium.ttf
        - asset: fonts/CircularStd-MediumItalic.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
